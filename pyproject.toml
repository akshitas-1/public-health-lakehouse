[project]
name = "public-health-lakehouse"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Akshita Sharma", email = "akshitas@uw.edu" }
]
requires-python = ">=3.13"
dependencies = [
    "databricks-sdk>=0.140.0",
    "pandas>=3.0.6",
    "pyarrow>=25.0.1",
    "python-dotenv>=1.2.3",
    "requests>=2.34.2",
]

[project.scripts]
public-health-lakehouse = "public_health_lakehouse:main"

[build-system]
requires = ["uv_build>=0.12.17,<0.13.0"]
build-backend = "uv_build"
